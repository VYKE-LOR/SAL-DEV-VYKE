Config = {}

-- Delay after first character spawn before applying the reset.
Config.DelayMs = 3000

-- How many times to try resolving valid ground Z.
Config.MaxGroundChecks = 15

-- Wait between ground checks.
Config.GroundCheckIntervalMs = 100

-- Small offset above the resolved ground so the player does not clip.
Config.GroundOffset = 1.0
